{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"],
      "inputs": ["src/**/*"]
    },
    "dev": {
      "persistent": true
    }
  }
}
